name: Donna deterministic tests
'on':
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  offline-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python }}
      - name: Unit and subprocess contract tests (no model or service credentials)
        run: python -m unittest discover -s tests -v
      - name: Compile Python sources
        run: python -m compileall -q scripts tests
      - name: Diff whitespace check
        run: git diff --check
# Actions pinned to reviewed commit SHAs by the integrating developer on 2026-09-22:
#   actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 (v7.0.1)
#   actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 (v7.0.0)
# Remote CI observed green on 2026-09-23: runs 35825804078 (push, branch),
# 35825817189 (pull_request, branch) and 35826010874 (push, main after merge) —
# Python 3.10-3.13 in all three.
